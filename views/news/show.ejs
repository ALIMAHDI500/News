<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>  News App</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .post-container {
            max-width: 800px;
            margin: 2rem auto;
            padding: 2rem;
            background: rgb(214, 184, 85);
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        
        .post-header {
            border-bottom: 1px solid #660909;
            padding-bottom: 1rem;
            margin-bottom: 1.5rem;
        }
        
        .post-title {
            font-size: 2rem;
            margin-bottom: 0.5rem;
            color: #00801c;
        }
        
        .post-author {
            display:flex;
            color: #0804fd;
            font-size: 1.0em;
            justify-items: end
            
        }
        
        .post-category {
             display: flex;
            background-color: #26e7ce;
            border-radius:1px;
            color: #ff0000;
             justify-self: end;
            border: #007bff solid 1px;
             width:fit-content;
             padding:10px;
             margin: 10px;
        }
        
        .post-content {
            font-size: 1.1rem;
            line-height: 1.8;
            margin-bottom: 2rem;
        }
        
        .post-actions {
            display: flex;
            gap: 1rem;
            margin-top: 2rem;
            padding-top: 1rem;
            border-top: 1px solid #eee;
        }
        
        .back-link {
            display: inline-block;
            margin-top: 1rem;
            color: #007bff;
            text-decoration: none;
        }
        
        .back-link:hover {
            text-decoration: underline;
        }
        img{
          height: 200px;
          width: 200px;
        }

        .Photo{
          align-items: center;
        }
    </style>
</head>
<body>
    <%- include('../partials/_navbar') %>
    
 <% if(user){%>
  <div class="post-container">
            <h1 class="post-title"><%= post.title %></h1>
                  
             
                <span class="post-category"><%= post.category %></span>
            
        <div class="post-content">
            <%= post.text %>
        </div>
  <img src=" <%=post.imageLink%>" >
    
              
                 <div class="post-actions">
                        <form action="/news/<%= post._id %>?_method=DELETE" method="POST">
                       <button><a href="/news/<%= post._id %>/edit" class="btn-edit">Edit Post</a> </button> 
                        <button type="submit" class="btn-delete">Delete Post</button>
                     </form>
                 </div>

        </div>            
 <% } else { %>
   
  <% news.forEach(post => { %>

       <div class="post-container"> 
            <h1 class="post-title"><%= post.title %></h1>
        
              
                <span class="post-category"><%= post.category %></span>
            
        <div class="post-content">
            <%= post.text %>
        </div>

    <img src=" <%=post.imageLink%>"alt="imagepost"class="Photo"> <br>
        
     <%= post.Date %>
       <span class="post-author">By <%=post.author%></span>
          </div>     
  <% }) %>


<% } %>
  

</body>
</html>